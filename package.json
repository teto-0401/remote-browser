{
  "name": "remote-browser",
  "version": "1.0.0",
  "description": "VNC-based remote browser system for cloud VM (Oracle Free Tier)",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "npm run start:vnc && npm start",
    "install:env": "bash scripts/install.sh",
    "start:vnc": "bash scripts/start.sh"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}